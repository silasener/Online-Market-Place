version: '3.8'
services:
  spring_app:
    container_name: springApp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql_db:3306/OnlineMarketPlace
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: silasener35
    networks:
      - app-network

  react_app:
    container_name: reactApp
    build:
      context: /Users/silasener/WebstormProjects/online_market_place_frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

